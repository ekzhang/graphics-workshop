import { Emitter } from '../../../common/model/emitter';
import { BladePosition } from './blade-positions';
/**
 * @hidden
 */
export interface BladeEvents {
    change: {
        propertyName: 'positions';
        sender: Blade;
    };
    dispose: {
        sender: Blade;
    };
}
export declare class Blade {
    readonly emitter: Emitter<BladeEvents>;
    private disposable_;
    private positions_;
    constructor();
    get positions(): BladePosition[];
    set positions(positions: BladePosition[]);
    get disposed(): boolean;
    dispose(): void;
    private onDispose_;
}
