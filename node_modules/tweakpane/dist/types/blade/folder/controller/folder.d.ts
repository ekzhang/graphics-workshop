import { ViewProps } from '../../../common/model/view-props';
import { BladeController } from '../../common/controller/blade';
import { Blade } from '../../common/model/blade';
import { BladeRack } from '../../common/model/blade-rack';
import { Folder } from '../model/folder';
import { FolderProps, FolderView } from '../view/folder';
interface Config {
    expanded?: boolean;
    blade: Blade;
    props: FolderProps;
    viewProps: ViewProps;
    viewName?: string;
}
/**
 * @hidden
 */
export declare class FolderController extends BladeController<FolderView> {
    readonly bladeRack: BladeRack;
    readonly folder: Folder;
    readonly props: FolderProps;
    constructor(doc: Document, config: Config);
    get document(): Document;
    onDispose(): void;
    private onFolderBeforeChange_;
    private onTitleClick_;
    private applyRackChange_;
    private onRackAdd_;
    private onRackRemove_;
    private onRackLayout_;
    private onContainerTransitionEnd_;
}
export {};
