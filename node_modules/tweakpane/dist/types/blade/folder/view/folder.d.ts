import { ValueMap } from '../../../common/model/value-map';
import { ViewProps } from '../../../common/model/view-props';
import { View } from '../../../common/view/view';
import { Folder } from '../model/folder';
export declare type FolderProps = ValueMap<{
    title: string | undefined;
}>;
export interface Config {
    folder: Folder;
    props: FolderProps;
    viewProps: ViewProps;
    viewName?: string;
}
/**
 * @hidden
 */
export declare class FolderView implements View {
    readonly buttonElement: HTMLButtonElement;
    readonly containerElement: HTMLDivElement;
    readonly titleElement: HTMLElement;
    readonly element: HTMLElement;
    private readonly folder_;
    private readonly className_;
    constructor(doc: Document, config: Config);
    private applyModel_;
    private onFolderChange_;
}
