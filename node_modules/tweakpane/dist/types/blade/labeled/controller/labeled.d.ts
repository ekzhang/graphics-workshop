import { Controller } from '../../../common/controller/controller';
import { BladeController } from '../../common/controller/blade';
import { Blade } from '../../common/model/blade';
import { LabeledProps, LabeledView } from '../view/labeled';
interface Config<C> {
    blade: Blade;
    props: LabeledProps;
    valueController: C;
}
export declare class LabeledController<C extends Controller> extends BladeController<LabeledView> {
    readonly props: LabeledProps;
    readonly valueController: C;
    constructor(doc: Document, config: Config<C>);
    onDispose(): void;
}
export {};
