import { ArrayStyleListOptions, BladeParams, ObjectStyleListOptions } from '../common/api/types';
import { BladePlugin } from '../plugin';
export interface ListBladeParams<T> extends BladeParams {
    options: ArrayStyleListOptions<T> | ObjectStyleListOptions<T>;
    value: T;
    view: 'list';
    label?: string;
}
export declare const ListBladePlugin: BladePlugin<ListBladeParams<unknown>>;
