import { PluginRegistration } from '../../common/api/util';
import { FolderApi } from '../../folder/api/folder';
import { RootController } from '../controller/root';
import { PresetObject } from './preset';
export declare class RootApi extends FolderApi {
    /**
     * @hidden
     */
    constructor(controller: RootController);
    /**
     * Registers a plugin.
     * @param r The configuration of the plugin.
     */
    static registerPlugin(r: PluginRegistration): void;
    get element(): HTMLElement;
    /**
     * Imports a preset of all inputs.
     * @param preset The preset object to import.
     */
    importPreset(preset: PresetObject): void;
    /**
     * Exports a preset of all inputs.
     * @return An exported preset object.
     */
    exportPreset(): PresetObject;
    /**
     * Refreshes all bindings of the pane.
     */
    refresh(): void;
}
