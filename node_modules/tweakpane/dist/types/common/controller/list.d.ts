import { ListProps, ListView } from '../../input-binding/common/view/list';
import { Value } from '../model/value';
import { ViewProps } from '../model/view-props';
import { ValueController } from './value';
interface Config<T> {
    props: ListProps<T>;
    value: Value<T>;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class ListController<T> implements ValueController<T> {
    readonly value: Value<T>;
    readonly view: ListView<T>;
    readonly props: ListProps<T>;
    readonly viewProps: ViewProps;
    constructor(doc: Document, config: Config<T>);
    private onSelectChange_;
}
export {};
