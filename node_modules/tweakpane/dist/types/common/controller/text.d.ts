import { TextProps, TextView } from '../../input-binding/common/view/text';
import { Parser } from '../converter/parser';
import { Value } from '../model/value';
import { ViewProps } from '../model/view-props';
import { ValueController } from './value';
/**
 * @hidden
 */
export interface Config<T> {
    props: TextProps<T>;
    parser: Parser<T>;
    value: Value<T>;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class TextController<T> implements ValueController<T> {
    readonly props: TextProps<T>;
    readonly value: Value<T>;
    readonly view: TextView<T>;
    readonly viewProps: ViewProps;
    private parser_;
    constructor(doc: Document, config: Config<T>);
    private onInputChange_;
}
