import { ValueController } from '../../controller/value';
import { Value } from '../../model/value';
import { ViewProps } from '../../model/view-props';
import { SliderProps, SliderView } from '../view/slider';
interface Config {
    baseStep: number;
    props: SliderProps;
    value: Value<number>;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class SliderController implements ValueController<number> {
    readonly value: Value<number>;
    readonly view: SliderView;
    readonly viewProps: ViewProps;
    readonly props: SliderProps;
    private ptHandler_;
    private baseStep_;
    constructor(doc: Document, config: Config);
    private handlePointerEvent_;
    private onPoint_;
    private onKeyDown_;
}
export {};
