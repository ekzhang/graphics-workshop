import { ValueController } from '../../../common/controller/value';
import { Parser } from '../../../common/converter/parser';
import { Value } from '../../../common/model/value';
import { ViewProps } from '../../../common/model/view-props';
import { Color } from '../model/color';
import { PickedColor } from '../model/picked-color';
import { ColorTextView } from '../view/color-text';
interface Config {
    parser: Parser<number>;
    pickedColor: PickedColor;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class ColorTextController implements ValueController<Color> {
    readonly pickedColor: PickedColor;
    readonly view: ColorTextView;
    readonly viewProps: ViewProps;
    private parser_;
    private ccs_;
    constructor(doc: Document, config: Config);
    get value(): Value<Color>;
    private createComponentControllers_;
    private onModeSelectChange_;
}
export {};
