import { Constraint } from '../../../common/constraint/constraint';
import { ValueController } from '../../../common/controller/value';
import { Parser } from '../../../common/converter/parser';
import { Value } from '../../../common/model/value';
import { ViewProps } from '../../../common/model/view-props';
import { NumberTextProps } from '../../../common/number/view/number-text';
import { Point2d } from '../model/point-2d';
import { Point2dPadTextView } from '../view/point-2d-pad-text';
interface Axis {
    baseStep: number;
    constraint: Constraint<number> | undefined;
    textProps: NumberTextProps;
}
interface Config {
    axes: [Axis, Axis];
    invertsY: boolean;
    maxValue: number;
    parser: Parser<number>;
    value: Value<Point2d>;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class Point2dPadTextController implements ValueController<Point2d> {
    readonly value: Value<Point2d>;
    readonly view: Point2dPadTextView;
    readonly viewProps: ViewProps;
    private readonly padIc_;
    private readonly textIc_;
    constructor(doc: Document, config: Config);
    private onPadButtonBlur_;
    private onPadButtonClick_;
}
export {};
