import { ValueController } from '../../../common/controller/value';
import { PrimitiveValue } from '../../../common/model/primitive-value';
import { Value } from '../../../common/model/value';
import { ViewProps } from '../../../common/model/view-props';
import { Point2d } from '../model/point-2d';
import { Point2dPadView } from '../view/point-2d-pad';
interface Config {
    baseSteps: [number, number];
    invertsY: boolean;
    maxValue: number;
    value: Value<Point2d>;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class Point2dPadController implements ValueController<Point2d> {
    readonly expanded: PrimitiveValue<boolean>;
    readonly value: Value<Point2d>;
    readonly view: Point2dPadView;
    readonly viewProps: ViewProps;
    triggerElement: HTMLElement | null;
    private readonly baseSteps_;
    private readonly ptHandler_;
    private readonly invertsY_;
    private readonly maxValue_;
    constructor(doc: Document, config: Config);
    private handlePointerEvent_;
    private onPointerDown_;
    private onPointerMove_;
    private onPointerUp_;
    private onPadKeyDown_;
    private onFocusableElementBlur_;
    private onKeyDown_;
}
export {};
