import { ValueController } from '../../../common/controller/value';
import { Formatter } from '../../../common/converter/formatter';
import { Buffer, BufferedValue } from '../../../common/model/buffered-value';
import { ViewProps } from '../../../common/model/view-props';
import { GraphLogView } from '../view/graph-log';
interface Config {
    formatter: Formatter<number>;
    lineCount: number;
    maxValue: number;
    minValue: number;
    value: BufferedValue<number>;
    viewProps: ViewProps;
}
/**
 * @hidden
 */
export declare class GraphLogController implements ValueController<Buffer<number>> {
    readonly value: BufferedValue<number>;
    readonly view: GraphLogView;
    readonly viewProps: ViewProps;
    private cursor_;
    constructor(doc: Document, config: Config);
    private onGraphMouseLeave_;
    private onGraphMouseMove_;
    private onGraphPointerDown_;
    private onGraphPointerMove_;
    private onGraphPointerUp_;
}
export {};
